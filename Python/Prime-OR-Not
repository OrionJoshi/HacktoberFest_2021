# Program to check if a number is prime or not
# Take a variable
num = 5

# Define a flag variable
flag = False

# 0,1 are not prime numbers; we start from 2
if num > 1:
    for i in range(2, num):
        if (num % i) == 0:
            # if divisible, flag True
            flag = True
            break

# condition responses
if flag:
    print(num, "is not a prime number")
else:
    print(num, "is a prime number")
